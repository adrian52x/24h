package com.example.project24h.repos;

import com.example.project24h.models.Candidate;
import org.junit.After;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;

import static org.junit.jupiter.api.Assertions.*;


class CandidateRepoTest {

   /* @Autowired
    CandidateRepo candidateRepo;

    @Test
    public void addCandidate(){
        Candidate candidate = new Candidate("Adrian","Enachi",23);
        assertEquals(0,candidate.getId());
        candidateRepo.save(candidate);
        assertTrue(candidate.getId()>0);
    }*/

}